syntax = "proto3";

package userGRPC;

option go_package = "github.com/quangtran88/anifni-grpc/userGRPC";

message PingMessage {}
message PingResult {
  string message = 1;
}

service UserService {
  rpc Ping(PingMessage) returns (PingResult) {};
}